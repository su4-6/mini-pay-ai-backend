# 前后端联调记录

每次涉及外部 API、SSE 或事件的联调，复制 `联调记录模板.md` 建立独立记录。接口事实以 `contracts/` 下的 YAML 为准；本目录只记录执行证据与结论，不复制维护接口定义。

联调通过的最小条件：请求/响应与契约一致；成功、权限、字段校验、处理中或失败至少各验证一次；记录关联页面 ID、FR/BR/AC/TC、服务版本和 `requestId`；记录中不出现密码、验证码、Token、Cookie 或完整密钥。
